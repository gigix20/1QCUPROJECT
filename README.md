
- First, run the "Add 'role' to 'users' table" query in _docs/db/  
- To insert an admin user run the create_admin.php file in the browser  
- Enter that credential to the login.php page to access the admin page
